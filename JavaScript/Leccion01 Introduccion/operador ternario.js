// operador Ternario            
// console.log("");
// si la condicion es Verdarea, ejecuta la expresion 1, sino es verdadera ejecuta la operacion 2.

let resultado = (6>2) ? "verdadero" : "falso";
console.log(resultado);

    // EJERCICIO NUMERO PAR CON OPERADOR TERNARIO
let numero = 19;
resultado = (numero % 2 == 0) ? "Numero Par" : "Numero impar";
// resultado = (numero % 2 == 0) ? "if" : "else";
console.log(resultado);